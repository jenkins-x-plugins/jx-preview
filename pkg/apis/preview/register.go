package preview

const (
	// GroupName defines the API group identifier for Preview types.
	GroupName = "preview.jenkins.io"

	// Version is the Preview API group version
	Version = "v1alpha1"

	// GroupAndVersion is the Preview API Group and version
	GroupAndVersion = GroupName + "/" + Version
)
